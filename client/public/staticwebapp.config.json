{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/static/*"]
  },
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://epic-kick-api.azurewebsites.net/api/:path*"
    }
  ]
}
